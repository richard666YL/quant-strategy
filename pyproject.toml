[project]
name = "quant-strategy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jupyter>=1.1.1",
    "matplotlib>=3.11.1",
    "moomoo-api>=10.9.6908",
    "numpy>=2.5.1",
    "pandas>=3.0.5",
    "yfinance>=1.5.2",
]

[project.scripts]
quant-strategy = "quant_strategy:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
